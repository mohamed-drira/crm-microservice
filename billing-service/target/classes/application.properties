# TEMPLATE — placeholder only, no real config yet.
spring.application.name=billing-service

# TODO: server.port
# TODO: eureka.client.service-url.defaultZone (discovery)
# TODO: spring.config.import=configserver: (config-server)
# TODO: spring.datasource.* (business services only)
# TODO: spring.kafka.* (hr/payroll/billing/crm services only)
